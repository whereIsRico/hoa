# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hoa_dev
DB_USER=passage
DB_PASSWORD=change_me

# Server
PORT=3000
NODE_ENV=development

# JWT (generate a real random secret — e.g. `openssl rand -base64 48`)
JWT_SECRET=change_me
